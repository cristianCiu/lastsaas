export interface GuestPlan { id:string; locationId:string; planDate:string; servicePeriod:string; guestCount:number; source:string; isActual:boolean; notes?:string; version:number; createdAt:string; updatedAt:string }
export interface ForecastPolicy { id:string; locationId:string; name:string; horizonDays:number; lookbackDays:number; safetyStockDays:number; isActive:boolean; version:number; createdAt:string; updatedAt:string }
export interface ForecastRun { id:string; locationId:string; datasetId:string; policyId:string; status:'pending'|'running'|'succeeded'|'failed'; algorithm:string; algorithmVersion:string; error?:string; createdAt:string; completedAt?:string }
export interface ForecastPoint { id:string; itemId:string; targetDate:string; forecastMicros:string; lowerMicros:string; upperMicros:string }
export interface ForecastMaturity { itemId:string; quality:{ flags:string[]; observedDays:number; expectedDays:number; missingDays:number; closureDays:number; coveragePermille:number }; flags:string[] }
export interface ForecastRecommendation { id:string; itemId:string; modelId:string; algorithmVersion:string; horizonDays:number; p90DemandMicros:string; safetyDemandMicros:string; usableStockMicros:string; confirmedInboundMicros:string; requestedQuantityMicros:string; roundedQuantityMicros:string; quantityMicros:string; packSizeMicros:string; moq:number; packMoqDeltaMicros:string; status:'ready'|'no-reorder-needed'; qualityFlags:string[]; observedDays:number; expectedDays:number; coveragePermille:number; maturity:string; formula:string; reasonCodes:string[]; createdAt:string }
export interface ForecastCoverage extends Omit<ForecastRecommendation,'status'|'quantityMicros'|'packSizeMicros'|'moq'|'packMoqDeltaMicros'> { projectedAvailableMicros:string; coverageDays:number }
